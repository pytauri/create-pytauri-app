[project]
name = "create-pytauri-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pytauri == 0.7.*",
    "pydantic == 2.*",
    "anyio == 4.*",
    # [build-system]
    "setuptools >= 80",
    "setuptools-scm >= 8",
]

[tool.uv]
required-version = ">=0.8.14"
